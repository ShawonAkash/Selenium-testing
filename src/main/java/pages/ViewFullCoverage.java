package pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class ViewFullCoverage {
    private WebDriver driver;

    public ViewFullCoverage(WebDriver driver) {
        this.driver = driver;
    }
}
